apiVersion: apps/v1
kind: Deployment
metadata:
  name: selenium-hub-stop
  labels:
    app: selenium-hub-stop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: selenium-hub-stop
  template:
    metadata:
      labels:
        app: selenium-hub-stop
    spec:
      containers:
      - name: selenium-hub-stop
        image: selenium/hub:3.141
        ports:
          - containerPort: 4444
        # resources:
        #   limits:
        #     memory: "1000Mi"
        #     cpu: ".5"
        livenessProbe:
          httpGet:
            path: /wd/hub/status
            port: 4444
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /wd/hub/status
            port: 4444
          initialDelaySeconds: 5
          timeoutSeconds: 1
